apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: postgresql-demo

resources:
  - namespace.yaml
  - network-policy.yaml
  - resource-quota.yaml
  - limit-range.yaml
  - ../components/secure-pv-bootstrap
  - serviceaccount.yaml
  - service.yaml
  - statefulset.yaml
